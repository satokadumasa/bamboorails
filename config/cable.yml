development:
  # adapter: async
  adapter: redis
  url: redis://localhost:6379
  channel_prefix: bamboo_production

test:
  adapter: async

production:
  # adapter: async
  adapter: redis
  url: redis://localhost:6379
  channel_prefix: bamboo_production
